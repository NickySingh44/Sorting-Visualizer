import React from 'react'
const no_Of_array_bars = 310;

function randomIntFromInterval(min,max){
    return Math.floor(Math.random() * (max - min + 1) + min);
}
const Array = () => {
  return (
    <>
    <h1>hello</h1>
    </>
  )
}

export default Array